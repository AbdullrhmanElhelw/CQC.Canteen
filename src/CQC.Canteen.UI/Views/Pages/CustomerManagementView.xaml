<UserControl x:Class="CQC.Canteen.UI.Views.Pages.CustomerManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vmpages="clr-namespace:CQC.Canteen.UI.ViewModels.Pages"
             xmlns:converters="clr-namespace:CQC.Canteen.UI.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vmpages:CustomerManagementViewModel}"
             Background="#F4F4F4"
             FlowDirection="RightToLeft"
             FontFamily="Segoe UI, Tahoma">

    <UserControl.Resources>
        <Color x:Key="EgyptianRedColor">#B22222</Color>
        <Color x:Key="EgyptianBlueColor">#003366</Color>
        <SolidColorBrush x:Key="EgyptianRedBrush" Color="{StaticResource EgyptianRedColor}"/>
        <SolidColorBrush x:Key="EgyptianBlueBrush" Color="{StaticResource EgyptianBlueColor}"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:BoolToTextConverter x:Key="BoolToTextConverter" TrueText="عسكري" FalseText="مدني"/>

        <!-- أزرار -->
        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="{StaticResource EgyptianRedBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="Button" x:Key="SecondaryButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource EgyptianBlueBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource EgyptianBlueBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- DataGrid حديث -->
        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="RowHeight" Value="42"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EEE"/>
        </Style>

        <Style x:Key="ColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource EgyptianBlueBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#F0F8FF"/>
                    <Setter Property="Foreground" Value="{StaticResource EgyptianBlueBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- شريط الأدوات -->
        <Border Background="White" CornerRadius="6" Padding="16" Margin="12"
                BorderBrush="#E1E1E1" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ إضافة عميل" Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding ShowAddCustomerDialogCommand}"/>
                <Button Content="✏️ تعديل العميل" Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding ShowEditCustomerDialogCommand}" Margin="8,0,0,0"/>
                <Button Content="💰 تسوية الرصيد" Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding SettleBalanceCommand}" Margin="8,0,0,0"/>
                <Button Content="🔄 إعادة تحميل" Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding LoadCustomersCommand}" Margin="8,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- الجدول -->
        <Border Grid.Row="1" Background="White" CornerRadius="6" Margin="12"
                BorderBrush="#E1E1E1" BorderThickness="1" Padding="8">
            <DataGrid ItemsSource="{Binding Customers}"
                      SelectedItem="{Binding SelectedCustomer}"
                      Style="{StaticResource ModernDataGridStyle}"
                      ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}"
                      CellStyle="{StaticResource DataGridCellStyle}"
                      IsReadOnly="True">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTextColumn Header="النوع" Binding="{Binding IsMilitary, Converter={StaticResource BoolToTextConverter}}" Width="1*"/>
                    <DataGridTextColumn Header="الرتبة" Binding="{Binding Rank}" Width="1*"/>
                    <DataGridTextColumn Header="الرصيد الحالي" Binding="{Binding CurrentBalance, StringFormat='{}{0:C} ج.م'}" Width="1.2*"/>
                    <DataGridCheckBoxColumn Header="نشط" Binding="{Binding IsActive}" Width="0.8*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- مؤشر التحميل -->
        <Border Background="#AAFFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="8" Margin="0,0,0,12"/>
                <TextBlock Text="جاري تحميل العملاء..." FontSize="14" FontWeight="SemiBold"
                           Foreground="{StaticResource EgyptianBlueBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
