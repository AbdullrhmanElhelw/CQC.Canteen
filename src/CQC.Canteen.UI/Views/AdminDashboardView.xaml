<UserControl x:Class="CQC.Canteen.UI.Views.AdminDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F4F4F4"
             FlowDirection="RightToLeft">

    <UserControl.Resources>

        <!-- 🎨 الألوان -->
        <Color x:Key="EgyptianRedColor">#B22222</Color>
        <Color x:Key="EgyptianGoldColor">#D4AF37</Color>
        <SolidColorBrush x:Key="EgyptianRedBrush" Color="{StaticResource EgyptianRedColor}"/>
        <SolidColorBrush x:Key="EgyptianGoldBrush" Color="{StaticResource EgyptianGoldColor}"/>

        <!-- 💡 ستايل عام للأزرار -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <TextBlock Text="{TemplateBinding Content}"
                                       Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#AAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 🎯 القائمة الجانبية -->
        <Border Grid.Column="0"
                Background="{StaticResource EgyptianRedBrush}">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.3"/>
            </Border.Effect>

            <StackPanel Margin="0,20,0,0">
                <!-- اللوجو -->
                <Image Source="/Assets/Logo (1) (1).png"
                       Width="100" Height="100" HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

                <!-- إدارة الأصناف -->
                <Button Content="🏷️ إدارة الأصناف"
                        Command="{Binding ShowProductsCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsProductsActive}" Value="True">
                                    <Setter Property="Background" Value="#22FFFFFF"/>
                                    <Setter Property="Foreground" Value="{StaticResource EgyptianGoldBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- إدارة العملاء -->
                <Button Content="👥 إدارة العملاء (الآجل)"
                        Command="{Binding ShowCustomersCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCustomersActive}" Value="True">
                                    <Setter Property="Background" Value="#22FFFFFF"/>
                                    <Setter Property="Foreground" Value="{StaticResource EgyptianGoldBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- إدارة الفئات -->
                <Button Content="🗂️ إدارة الفئات"
                        Command="{Binding ShowCategoriesCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCategoriesActive}" Value="True">
                                    <Setter Property="Background" Value="#22FFFFFF"/>
                                    <Setter Property="Foreground" Value="{StaticResource EgyptianGoldBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- باقي الأزرار -->
                <Button Content="👤 إدارة المستخدمين"
                        Style="{StaticResource NavButtonStyle}"
                        IsEnabled="False"/>

                <Button Content="📊 التقارير"
                        Style="{StaticResource NavButtonStyle}"
                        IsEnabled="False"/>
            </StackPanel>
        </Border>

        <!-- 🧩 منطقة المحتوى -->
        <Border Grid.Column="1" Padding="20" Background="#F9F9F9">
            <ContentControl Content="{Binding CurrentAdminPageViewModel}" />
        </Border>
    </Grid>
</UserControl>
