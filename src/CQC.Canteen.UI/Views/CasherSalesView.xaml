<UserControl x:Class="CQC.Canteen.UI.Views.CasherSalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="#F9FAFB"
             d:DesignHeight="700" d:DesignWidth="1100"
             FlowDirection="RightToLeft"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style x:Key="TextBoxPlaceholder" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <Grid>
                                <TextBlock Text="ابحث عن منتج..."
                                           Foreground="DarkGray"
                                           FontWeight="Light"
                                           Margin="5,0,0,0"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Left"
                                           Visibility="Collapsed"
                                           x:Name="PlaceholderText"/>

                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#B22222" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" CornerRadius="10" Background="White" Padding="15" Margin="5" BorderBrush="#EEE" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="{Binding CartItems.Count, StringFormat='🛍️ سلة المشتريات ({0})'}" 
                           FontSize="20" FontWeight="Bold" Foreground="#B22222" Margin="0,0,0,15"/>

                <DataGrid ItemsSource="{Binding CartItems}" AutoGenerateColumns="False" CanUserAddRows="False"
                          HeadersVisibility="Column" RowHeaderWidth="0" Margin="0,0,0,10" Height="400">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الصنف" Binding="{Binding Name}" Width="*"/>
                        <DataGridTemplateColumn Header="الكمية" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <Button Content="-" 
                                                Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CommandParameter="{Binding}" 
                                                Width="25" Height="25" Padding="0"
                                                Background="#FFCDD2" Foreground="#B71C1C" FontWeight="Bold" BorderThickness="0"/>
                                        <TextBlock Text="{Binding Quantity}" Margin="8,0" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <Button Content="+" 
                                                Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CommandParameter="{Binding}" 
                                                Width="25" Height="25" Padding="0"
                                                Background="#C8E6C9" Foreground="#1B5E20" FontWeight="Bold" BorderThickness="0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="السعر" Binding="{Binding Price, StringFormat={}{0:N2}}" Width="90" />
                        <DataGridTextColumn Header="الإجمالي" Binding="{Binding Total, StringFormat={}{0:N2}}" Width="90"/>
                        <DataGridTemplateColumn Width="40">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="🗑️"
                                            Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5">
                    <TextBlock Margin="0,0,20,0" VerticalAlignment="Center">
                        <Hyperlink Command="{Binding ClearCartCommand}" Foreground="#DC3545" TextDecorations="None">
                            <TextBlock Text="🗑️ إفراغ السلة"/>
                        </Hyperlink>
                    </TextBlock>
                    <TextBlock Text="الإجمالي:" FontWeight="Bold" FontSize="16" Margin="5,0"/>
                    <TextBlock x:Name="TotalText"
                               Text="{Binding TotalAmount, StringFormat={}{0:N2}}"
                               FontSize="16"
                               Foreground="#B22222"
                               FontWeight="Bold"/>
                    <TextBlock Text=" ج.م" FontSize="14" Margin="2,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="💵 دفع نقدي" Command="{Binding PayCashCommand}"
                            Background="#B22222" Foreground="White" FontSize="16" Width="150" Margin="0,0,10,0"
                            Cursor="Hand" BorderThickness="0" Padding="10,5" />
                    <Button Content="🧾 دفع آجل" Command="{Binding PayDeferredCommand}"
                            Background="#D4AF37" Foreground="Black" FontSize="16" Width="150"
                            Cursor="Hand" BorderThickness="0" Padding="10,5" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" CornerRadius="10" Background="White" Padding="15" Margin="5" BorderBrush="#EEE" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="🧺 المنتجات المتاحة" FontSize="20" FontWeight="Bold" Foreground="#B22222" Margin="0,0,0,15"/>

                <DockPanel Grid.Row="1" Margin="0,0,0,15">
                    <TextBox DockPanel.Dock="Right" Width="250" Height="35" Padding="5"
                             Text="{Binding ProductSearchText, UpdateSourceTrigger=PropertyChanged}"
                             VerticalContentAlignment="Center" FontSize="14"
                             BorderBrush="#CCC" BorderThickness="1"
                             Background="#F9F9F9"
                             Style="{StaticResource TextBoxPlaceholder}"/>
                    <ComboBox Width="200" Height="35"
                              Margin="10,0,0,0"
                              ItemsSource="{Binding Categories}"
                              SelectedItem="{Binding SelectedCategory, UpdateSourceTrigger=PropertyChanged}"
                              DisplayMemberPath="Name"
                              FontSize="14"
                              Background="#F9F9F9"
                              BorderBrush="#CCC" BorderThickness="1"
                              Text="اختر الفئة..." 
                              IsEditable="True"
                              IsReadOnly="True"/>
                </DockPanel>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding GroupedProductsView}">
                        <ItemsControl.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <Border Background="#B22222" CornerRadius="5" Margin="0,15,0,5" Padding="10,5">
                                            <TextBlock Text="{Binding Name}"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       Foreground="White"/>
                                        </Border>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ItemsControl.GroupStyle>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="160" Height="160" 
                                        Margin="10" Padding="10"
                                        Background="#F9F9F9" CornerRadius="8" BorderBrush="#DDD" BorderThickness="1">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding SalePrice, StringFormat={}{0} ج.م}" Foreground="#B22222" FontSize="14" Margin="0,5,0,0" HorizontalAlignment="Center"/>

                                        <TextBlock Text="{Binding StockQuantity, StringFormat='المتاح: {0}'}" 
                                                   FontSize="12" 
                                                   HorizontalAlignment="Center"
                                                   Margin="0,5,0,5">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding StockQuantity}" Value="0">
                                                            <Setter Property="Foreground" Value="#D32F2F"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <TextBlock Text="{Binding CategoryName}" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                                        <Button Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Width="100" Height="30" BorderThickness="0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Content" Value="➕ إضافة"/>
                                                    <Setter Property="Background" Value="#B22222"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="Cursor" Value="Hand"/>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter Property="Content" Value="🚫 نفذت الكمية"/>
                                                            <Setter Property="Background" Value="#9E9E9E"/>
                                                            <Setter Property="Cursor" Value="No"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" IsItemsHost="True"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>