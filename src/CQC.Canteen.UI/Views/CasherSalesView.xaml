<UserControl x:Class="CQC.Canteen.UI.Views.CasherSalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#F5F6FA"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <!-- 🎨 الألوان -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#B22222"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#D4AF37"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF"/>

        <!-- 🧱 ستايل زر المنتج -->
        <Style x:Key="ProductButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="100"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <ContentPresenter/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFF2F2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#E8E8E8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 💰 ستايل الأزرار الأساسية -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#9A1B1B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 🔍 شريط البحث -->
        <DockPanel Grid.Column="0" Grid.Row="0" Margin="0,0,15,10">
            <TextBox x:Name="SearchBox"
                     Width="400"
                     Height="36"
                     Padding="10"
                     FontSize="14"
                     VerticalContentAlignment="Center"
                     BorderBrush="#CCC"
                     BorderThickness="1"
                     Tag="ابحث عن الصنف..."
                     Text="ابحث عن الصنف..."
                     Foreground="#888"
                     GotFocus="SearchBox_GotFocus"
                     LostFocus="SearchBox_LostFocus"
                     />

            <Button Content="🔎 بحث"
                    Width="100"
                    Height="36"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="10,0,0,0"/>
        </DockPanel>

        <!-- 🧃 شبكة الأصناف -->
        <ScrollViewer Grid.Column="0" Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Products}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource ProductButtonStyle}"
                                Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" 
                                           FontWeight="Bold"
                                           FontSize="14"
                                           TextAlignment="Center"
                                           Foreground="#333"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding SalePrice, StringFormat='{}{0:0.00} ج.م'}"
                                           FontSize="12"
                                           Foreground="#666"
                                           TextAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 🧾 الفاتورة -->
        <Border Grid.Column="1" Grid.RowSpan="2" 
                Background="White" 
                CornerRadius="10" 
                BorderBrush="#DDD" 
                BorderThickness="1" 
                Padding="15">
            <StackPanel>
                <TextBlock Text="🧾 الفاتورة الحالية"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrush}"
                           Margin="0,0,0,10"/>

                <ListView ItemsSource="{Binding CartItems}" 
                          Height="400" 
                          Margin="0,0,0,10">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="المنتج" Width="130" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="الكمية" Width="60" DisplayMemberBinding="{Binding Quantity}"/>
                            <GridViewColumn Header="السعر" Width="80" DisplayMemberBinding="{Binding Price}"/>
                            <GridViewColumn Header="الإجمالي" Width="80" DisplayMemberBinding="{Binding Total}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,15">
                    <TextBlock Text="الإجمالي:" 
                               FontWeight="Bold" 
                               FontSize="16" 
                               Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding TotalAmount}" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource PrimaryBrush}"/>
                </StackPanel>

                <Button Content="💰 إتمام العملية"
                        Height="45"
                        Style="{StaticResource PrimaryButtonStyle}"
                        FontSize="16"
                        FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
